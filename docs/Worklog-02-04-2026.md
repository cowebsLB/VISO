# Worklog — 02-04-2026

## Tasks completed

- **Inventory admin (`/admin/inventory`):** Replaced adjustment-only UX with full staff flows: add ingredient (modal), edit metadata (on-hand via separate stock flow), update stock via **`adjust_inventory`** RPC, remove with FK-aware errors. Wired mutations through **`getStaffSupabase()`** and a typed **`withStaff`** helper; fixed **`runWithStaff`** → **`withStaff`** consistency and delete **`await`** returning **`{ error }`**.
- **Staff login (`/admin/login`):** Redirect to **`/admin`** immediately after successful **`signInWithPassword`**; **`AdminAuthGate`** **`onAuthStateChange`** now redirects signed-in users off the login page (previously only initial **`getSession()`** did, so a reload was required).
- **Gmail / real emails:** Added **`resolveStaffAuthEmail`** — values containing **`@`** sign in as that full email; short names still map to **`@viso-admin.local`**. Updated copy, placeholders, and error messages (including email-not-confirmed).
- **Sign out:** **`signOut({ scope: "local" })`** to avoid **403** on global logout with the anon key.
- **Recipes / BOM (`/admin/recipes`):** Rebuilt read UI — grouped by product and variant with English labels from i18n tables; then added **Add ingredient**, **Edit**, **Remove** with modal CRUD on **`recipe_lines`**, duplicate-key messaging, and documentation of variant vs fallback behavior aligned with **`apply_order_to_inventory`**.
- **Dev hygiene:** **`scripts/clean-next.mjs`**, **`npm run clean`**, **`experimental.devtoolSegmentExplorer: false`** in **`next.config.ts`** (from earlier same sprint; documented).
- **Documentation:** Added **`docs/index.md`** hub; split technical detail into **`admin-auth-and-login.md`**, **`admin-inventory.md`**, **`admin-recipes-bom.md`**, **`dev-environment-windows.md`**; updated **`seed-admins.md`**, **`catalog-storage-and-staff.md`**, **`implementation-chronicle.md`** (Part I); this worklog.

## Problems encountered

- Login appeared successful only after **manual reload** — root cause: auth state listener did not navigate on **SIGNED_IN** from the login page.
- **400** on token endpoint when using **Gmail**-created Auth users while typing only the short local part — root cause: app sent **`local@viso-admin.local`** instead of the real Auth email; fixed by **`resolveStaffAuthEmail`** and UI guidance.
- **403** on **`/auth/v1/logout?scope=global`** — mitigated with **local** sign-out scope.
- Recipes table was a flat unreadable list of slugs — addressed with grouping, i18n labels, then CRUD.

## Decisions made

- Keep **single** Supabase browser client singleton for GoTrue (existing pattern).
- Treat **BOM** edits as direct **`recipe_lines`** DML from admin (no new RPC) to stay close to Table Editor behavior while improving UX.
- Split **docs** into topic files plus **`docs/index.md`** instead of one monolith (chronicle remains narrative history).

## Next steps (optional)

- E2E coverage for recipe line add/edit/delete and login redirect.
- “Copy BOM to all variants” or bulk actions if operations ask for it.
- Locale switcher on recipes page (en/ar/hy) if non-English staff labels are needed.
