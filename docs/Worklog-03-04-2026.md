# Worklog — 03-04-2026

## Tasks completed

- **Branding (Anush Badar):** Replaced remaining **VISO** user-facing strings with **`BRAND_NAME`** / neutral identifiers; **`package.json`** name **`anushbadar-bakery`**; **`docs/index.md`**, **`.env.example`**, migration header comment; documented legacy **`viso-admin.local`** in **`staff-email.ts`** (do not change without Auth migration).
- **Browser storage:** Migrated **`anushbadar-cart`**, **`anushbadar-locale`**, admin notification prefs from legacy **`viso-*`** keys with read-through copy in **`CartContext`**, **`LocaleContext`**, **`StaffOrderNotifications`**.
- **PWA:** Strengthened **`manifest.ts`** (`id`, `scope`, `categories`); **`layout.tsx`** explicit **`manifest`** link; **`PwaInstallBanner`** (Share → Add to Home Screen on iOS in production; **`beforeinstallprompt`** on Android); dev hides noisy iOS strip on localhost; **`globals.css`** **`site-fade-*`** / **`site-float`** keyframes.
- **Dev chunk 404s:** **`ClientProviders`** clears **Cache Storage** plus unregisters **service workers** on **`localhost`** / **`127.0.0.1`** so stale Serwist precache does not break **`/_next/static/*`** (admin included); README troubleshooting updated.
- **Staff notifications:** **`StaffOrderNotifications`** + **`SITE_SHOW_NOTIFICATION`** in **`sw.ts`** / **`post-sw-notification.ts`**; **`postShowNotificationViaSw`** gated to **`/admin`** paths only.
- **Admin orders (`/admin/orders`):** **All orders** list as **cards** below **`xl`**, **table** at **`xl`+**, matching **Menu** responsive pattern; status/source formatting; loading/empty states outside the table.
- **Mobile storefront:** **`overflow-x-hidden`**, **`min-w-0`**, hero/grid tweaks (**`page.tsx`**, **`SiteShell`**, **`Header`**) from prior session in this commit batch.

## Problems encountered

- **`/_next/static/chunks/...` 404** on localhost admin — stale **Service Worker** + **Cache Storage** from static export / prod preview intercepting dev assets.
- **Manifest `id` ignored** in console when **`id`** origin does not match document (dev vs **`NEXT_PUBLIC_SITE_URL`**); acceptable for local testing.

## Decisions made

- Keep **GitHub Pages** path **`/VISO`** and **`cowebslb.github.io/VISO`** as deployment identifiers (not product name).
- **PWA install** UX: production-only auto **iOS** hint to avoid Chrome device-toolbar noise on **`localhost`**.

## Next steps (optional)

- E2E: admin orders card expand / detail actions on narrow viewport.
- Align **`manifest.id`** with dev **`metadataBase`** if console noise should be zero in local builds.
