# Worklog — 06-04-2026

## Tasks completed

- **Baker split feature (admin):** Added **`/admin/assigned-orders`** to split active order items by baker without changing checkout or order creation flow.
- **Assignment rules (v1):** Implemented shared mapper in **`src/lib/admin/baker-assignment.ts`**.
  - **Vicky:** `brioche/plain`, `brioche/chocolate`, all `maamoul*`, `armenian-gata`.
  - **Sonig:** everything else.
- **Admin navigation:** Added **Assigned Orders** entry to **`src/components/admin/AdminNav.tsx`**.
- **Orders UX improvement:** Replaced bottom-page details panel with modal in **`src/app/admin/(shell)/orders/page.tsx`**.
  - Includes item list + existing status actions (confirm / progress).
  - Supports close via backdrop click, **Esc**, and button.
- **No schema changes:** Kept assignments **derived** from existing `orders` + `order_items` for low risk and maintainability.
- **GitHub Pages/static-export sanity:** Verified static output still includes admin routes and build passes after changes.

## Problems encountered

- **Dev server instability and 500s:** Repeated Next.js runtime churn produced `/_next/static/*` 404, then `ENOENT` for **`.next/routes-manifest.json`**, causing `/` and `/admin/orders` 500s.
- **Port collision (`EADDRINUSE` on 3040):** stale `node.exe` continued listening on 3040 after failed restart attempts.
- **Browser extension noise:** `content.js: Feature is disabled` appeared in console and was unrelated to app runtime.

## Fixes applied

- **Cache/build-state reset:** Stop dev server → **`npm run clean`** (remove `.next` + `node_modules/.cache`) → restart dev.
- **Process cleanup:** identified listener via `netstat -ano | rg :3040`, killed stale node PID, restarted cleanly.
- **Verification after reset:** confirmed **`http://localhost:3040/`** and **`/admin/orders`** return `200` after cleanup.
- **Operational guardrail reinforced:** avoid running **`npm run build`** while **`npm run dev`** is running (both mutate `.next` and can corrupt manifests).

## Tests and validation run

- **Build (multiple runs):** `npm run build` passed with routes including:
  - `/admin/orders`
  - `/admin/assigned-orders`
- **Lint:** `npm run lint` passed with one pre-existing warning in `recipes/page.tsx` (hook deps), no new blocking errors.
- **Runtime route checks:** `Invoke-WebRequest` confirmed HTTP `200` on `/` and `/admin/orders` after process/cache recovery.
- **Functional checks (admin):**
  - Details opens as modal and keeps status transition actions.
  - Assigned Orders page compiles and renders in admin shell.

## Decisions made

- **Derived assignment model (for handoff):** Do not introduce new assignment table yet; compute from canonical order item data.
- **Rule source of truth:** Prefer `product_id` + `option_id`; keep title-based fallback for resilience.
- **Risk posture:** Avoid DB migration this round; prioritize stable handoff and preserving current production flow.

## Handoff notes / next steps

- If long-term per-baker history/analytics are needed, add a persisted table (e.g. `order_item_assignments`) in a separate migration with clear rollback.
- Add focused tests for:
  - assignment mapping edge cases,
  - `confirm_order` inventory application behavior,
  - status transition constraints.
