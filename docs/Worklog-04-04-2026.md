# Worklog — 04-04-2026

## Tasks completed

- **Admin Web Push (Supabase):** Migration **`admin_push_subscriptions`**; Edge Function **`send-order-push`**; **`supabase/config.toml`** **`verify_jwt = false`** for webhook auth; **`public/admin/sw.js`** **`push`** event handler; **`src/lib/push/admin-web-push.ts`**; **`StaffOrderNotifications`** registers push and polls in-tab as a fallback.
- **Order notifications follow-up:** Same **`order-<id>`** notification tag as Edge push; **`send-order-push`** accepts nested **`payload`**, relaxed INSERT/schema checks, and logs **`payload_mismatch`** / empty **`admin_push_subscriptions`**; register admin service worker in **`next dev`** (**`ClientProviders`**, **`StaffOrderNotifications`** SW ready).
- **Automation:** **`scripts/supabase-setup-order-push.mjs`** and **`npm run supabase:setup-order-push`** (VAPID + secrets + **`db push`** + function deploy); **`web-push`** devDependency; **`.gitignore`** for **`supabase/.push-setup-secrets.env`**.
- **Docs:** **`docs/admin-push-notifications.md`**, **`docs/supabase-step-by-step.md`** (Integrations → Webhooks); **`docs/index.md`**, **`README.md`**, **`admin-auth-and-login.md`**, **`admin-push-notifications.md`** cross-links; **`deploy-pages.yml`** optional **`NEXT_PUBLIC_VAPID_PUBLIC_KEY`**; **`.env.example`** VAPID + **`SITE_URL`** note.
- **Stability:** **`next.config.ts`** gate **`output: "export"`** to build/start only; **`src/lib/basePath.ts`** safe **`siteUrl`** for empty/invalid **`NEXT_PUBLIC_SITE_URL`**; **`LocalhostPwaCacheBustScript`**; **`tsconfig`** exclude **`supabase/functions`** from Next typecheck.

## Problems encountered

- **Database Webhooks** not visible under **Database →** in current Studio — moved to **Integrations → Webhooks**.
- Truncated Edge Function URL **`…/send-order-pu`** in webhook form → must be **`…/send-order-push`**.
- **`ORDER_PUSH_WEBHOOK_SECRET`** not re-readable in Dashboard after set — use local **`.push-setup-secrets.env`** or rotate and update webhook header.

## Decisions made

- One CLI script for push setup; single manual Dashboard step (webhook) plus **`NEXT_PUBLIC_VAPID_PUBLIC_KEY`** in app env.

## Next steps (optional)

- Confirm **GitHub** repo secret **`NEXT_PUBLIC_VAPID_PUBLIC_KEY`** for Pages builds.
- Smoke test push on production URL after deploy.
